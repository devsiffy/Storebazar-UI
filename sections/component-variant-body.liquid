{% assign all_components =  page.metafields.custom.all_components.type.size %}

{{ all_components }}
{% comment %} 
{% for component in all_components %}
  {% assign component_variant = component.component_variants.value %}
  {{ component_variant.title }}
{% endfor %} {% endcomment %}
